{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "$comment": "Commands for CLI-based deploys of the web app from the monorepo root. The filter builds @tieout/web plus its workspace dependencies (@tieout/schema, rules, workflow, api) so their dist output exists before next build.",
  "framework": "nextjs",
  "installCommand": "pnpm install",
  "buildCommand": "pnpm --filter @tieout/web... --workspace-concurrency=1 build"
}
