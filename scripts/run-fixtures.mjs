// OPS-05 will replace this placeholder with the deterministic 10-fixture runner.
// Keep the command available now so local and CI scripts can reserve the gate
// without requiring fixture data or production provider credentials.
console.log('Fixture validation is reserved for OPS-05.');
